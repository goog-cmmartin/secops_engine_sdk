---
id: prop-20260924094207-detect
title: 'Evaluation for ur_1e433bb3-65fd-44ce-a022-845f90ea47d0: Wiz Low Severity Alert
  (NO_TUNING_NEEDED)'
author: '@detection-tuning-agent'
subsystem: detections
target_resource_id: ur_1e433bb3-65fd-44ce-a022-845f90ea47d0
action_type: CREATE_FINDINGS_REFINEMENT
status: OPEN
risk_level: MEDIUM
created_at: '2026-09-24T09:42:07.798237+00:00'
updated_at: '2026-09-24T09:42:07.798237+00:00'
merged_at: null
merged_by: null
merge_commit: null
rejection_reason: null
preflight:
  syntax_verified: true
  compiler_diagnostics:
  - Curated Rule UDM Findings Refinement exclusion syntax verified.
  replay_verified: false
  replay_target_tenant: ''
  replay_log_count: 0
  replay_summary: ''
  details: {}
mutation_payload:
  rule_id: ur_1e433bb3-65fd-44ce-a022-845f90ea47d0
  action_type: CREATE_FINDINGS_REFINEMENT
  tuned_rule_text: "rule ttp_vendor_wiz_severity_low {\n  meta:\n    rule_name = \"\
    Wiz: Low Severity Alert\"\n    description = \"Surfacing Low severity vendor alerts\
    \ from Wiz\"\n    severity = \"Low\"\n\n  events:\n    $e.metadata.log_type =\
    \ \"WIZ_IO\"\n    $e.security_result.severity = \"LOW\"\n    ($e.metadata.product_event_type\
    \ = \"DETECTIONS\" or $e.additional.fields[\"trigger_source\"] = \"DETECTIONS\"\
    )\n    $entity_name = strings.coalesce(\n      $e.principal.hostname,\n      $e.principal.asset.hostname,\n\
    \      re.capture($e.principal.asset_id, \":(.*)\"),\n      re.capture($e.principal.asset.asset_id,\
    \ \":(.*)\"),\n      $e.principal.ip,\n      $e.principal.asset.ip,\n      $e.principal.user.user_display_name,\n\
    \      $e.principal.user.userid,\n      $e.principal.user.email_addresses,\n \
    \     $e.target.hostname,\n      $e.target.asset.hostname,\n      re.capture($e.target.asset_id,\
    \ \":(.*)\"),\n      re.capture($e.target.asset.asset_id, \":(.*)\"),\n      $e.target.ip,\n\
    \      $e.target.asset.ip,\n      $e.target.user.user_display_name,\n      $e.target.user.userid,\n\
    \      $e.target.user.email_addresses\n    )\n\n    $entity_name != \"\"\n\n \
    \   $threat_name = strings.coalesce(\n      $e.security_result.threat_name,\n\
    \      $e.security_result.rule_name,\n      $e.security_result.summary,\n    \
    \  $e.security_result.category_details,\n      $e.metadata.description,\n    \
    \  $e.metadata.product_event_type\n    )\n\n    $threat_name != \"\"\n\n  outcome:\n\
    \    $risk_score = 35\n    $vendor_name = \"Wiz\"\n    $product_name = \"\"\n\
    \    $log_source = array_distinct($e.metadata.log_type)\n    $parser_type = array_distinct($e.metadata.parser_version)\n\
    \    $event_count = count_distinct($e.metadata.id)\n    $entity = array_distinct($entity_name)\n\
    \    $threat = array_distinct($threat_name)\n    $security_result_action = array_distinct($e.security_result.action)\n\
    \    $security_result_action_details = array_distinct($e.security_result.action_details)\n\
    \    $security_result_category_details = array_distinct($e.security_result.category_details)\n\
    \    $security_result_description = array_distinct($e.security_result.description)\n\
    \    $security_result_rule_id = array_distinct($e.security_result.rule_id)\n \
    \   $security_result_rule_name = array_distinct($e.security_result.rule_name)\n\
    \    $security_result_severity_details = array_distinct($e.security_result.severity_details)\n\
    \    $security_result_summary = array_distinct($e.security_result.summary)\n \
    \   $security_result_threat_id = array_distinct($e.security_result.threat_id)\n\
    \    $security_result_threat_name = array_distinct($e.security_result.threat_name)\n\
    \    $principal_ip = array_distinct($e.principal.ip)\n    $principal_nat_ip =\
    \ array_distinct($e.principal.nat_ip)\n    $principal_port = array_distinct($e.principal.port)\n\
    \    $principal_hostname = array_distinct($e.principal.hostname)\n    $principal_user_userid\
    \ = array_distinct($e.principal.user.userid)\n    $principal_user_email_addresses\
    \ = array_distinct($e.principal.user.email_addresses)\n    $principal_location_country_or_region\
    \ = array_distinct($e.principal.location.country_or_region)\n    $target_ip =\
    \ array_distinct($e.target.ip)\n    $target_nat_ip = array_distinct($e.target.nat_ip)\n\
    \    $target_port = array_distinct($e.target.port)\n    $target_hostname = array_distinct($e.target.hostname)\n\
    \    $target_user_userid = array_distinct($e.target.user.userid)\n    $target_application\
    \ = array_distinct($e.target.application)\n    $target_url = array_distinct($e.target.url)\n\
    \    $target_location_country_or_region = array_distinct($e.target.location.country_or_region)\n\
    \    $metadata_product_event_type = array_distinct($e.metadata.product_event_type)\n\
    \    $metadata_description = array_distinct($e.metadata.description)\n    $network_direction\
    \ = array_distinct($e.network.direction)\n\n    $result_time = min($e.metadata.event_timestamp.seconds)\n\
    \    $tmp1 = max(\n      if($e.security_result.action != \"BLOCK\" and $e.security_result.action\
    \ != \"UNKNOWN_ACTION\", 2)\n    )\n    $tmp2 = max(\n      if($e.security_result.action\
    \ = \"BLOCK\", 1)\n    )\n    $result = arrays.index_to_str(strings.split(\"attempted,failed,succeeded,succeeded\"\
    ), $tmp1 + $tmp2)\n    $update_detection = true\n    $update_managed_defense_aggregations\
    \ = true\n\n    $signature_name = array_distinct($threat_name)\n    $rule_category\
    \ = \"vendor\"\n    $event_min_timestamp = min($e.metadata.event_timestamp.seconds\
    \ * 1000000000 + $e.metadata.event_timestamp.nanos)\n    $event_max_timestamp\
    \ = max($e.metadata.event_timestamp.seconds * 1000000000 + $e.metadata.event_timestamp.nanos)\n\
    \n    $name_override = strings.concat(\n      \"Wiz: Low Severity Alert - \",\n\
    \      arrays.join_string($threat, \",\")\n    )\n\n  condition:\n    $e\n}"
  unsuppressed_trigger_count: 380
  projected_suppressed_count: 0
  noise_reduction_pct: 0
  preserved_real_alerts: 380
---

# Evaluation for ur_1e433bb3-65fd-44ce-a022-845f90ea47d0: Wiz Low Severity Alert (NO_TUNING_NEEDED)

## Problem Rationale
Diversity check failed: The top firing entity pattern accounts for only 3.4% (13/380) of detections, well below the 20% dominance threshold. Triggers are evenly dispersed across external IP addresses and target assets without a recurring benign administrative baseline. Applying broad exclusions would create blind spots. Classified as NO_TUNING_NEEDED.

## Proposed Unified Diff
```diff
# No diff: Exclusions withheld per blinding prevention guardrails
```

## Pre-Flight Verification Proof
- Syntax Verified: True
- Replay Verified: False
- Replay Log Count: 0
- Replay Summary: 
